**Setup**

1. Modify **config.py**
2. Create virtual environment **python -m venv env**
3. Activate virtual environment **.\\env\\Scripts\\activate.bat**
4. Install Dependencies **pip install -r requirements.txt**
5. Run app **streamlit.cmd run trader.py**

**Notes**

Place the strategies under the Strategies directory. It supports subfolders.

