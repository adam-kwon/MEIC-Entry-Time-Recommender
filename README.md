**Setup**

1. Create virtual environment **python -m venv env**
2. Activate virtual environment (.bat is for Windows, so choose the appropriate script for your OS/Shell) **.\\env\\Scripts\\activate.bat**
3. Install Dependencies **pip install -r requirements.txt**
4. Run app **streamlit.cmd run trader.py**
